<div class="container">
  <h1 class="mb-4">All Your JPop News!</h1>

  {{#each articles}}
  <div class="list-group mb-4">
    <a href="{{this.link}}" target="_blank" class="list-group-item list-group-item-action flex-column align-items-start">
      <div class="d-flex w-100 justify-content-between">
        <h5 class="mb-1">{{this.title}}</h5>
        <small>{{this.label}}</small>
      </div>
      <img src={{this.imgLink}}>
      <p class="mb-1">{{this.entryDesc}}</p>
    </a>
  </div>
  {{/each}}
  
</div>